<!DOCTYPE html>
<html lang="en">
 <%- include('../partials/head'); %>

<body class="container">
    <%- include('../partials/header') %>
    <main>
        <div class="jumbotron">
            <h1>Node com EJS</h1>
            <p>Vamos aprender a trabalhar com arquivos EJS</p>
        </div>

        <ul>
            <% texts.forEach(function(texts) { %>
                <li>
                    <strong>Título: <%= texts.title %> </strong> <p>Message: <%= texts.message %> </p> 
                </li>
            <% }); %>
        </ul>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>